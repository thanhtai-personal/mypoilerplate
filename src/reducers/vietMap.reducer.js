// import actionType from '../actionTypes'
import geography from '../data/vietMap/VietNam.json'

const initialState = {
  geography: geography
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}